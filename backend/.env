PORT=5000
NODE_ENV=development

# Database Configuration
# Update these with your MySQL credentials
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=eventflow

# JWT Configuration - Change this to a strong random string for production
JWT_SECRET=eventflow_super_secret_key_change_this_in_production_12345

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
